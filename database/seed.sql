-- Optional seed data for development
-- Note: User seeding typically requires creating auth.users entries first.
-- In a real scenario, use Supabase CLI or create accounts via the UI first.

-- Example ticket types insert (assuming event exists):
-- INSERT INTO public.ticket_types (event_id, name, description, price, total_quantity, available_quantity)
-- VALUES ('<EVENT_UUID>', 'General Admission', 'Standard entry ticket', 10.00, 100, 100);
